apiVersion: v1
kind: Secret
metadata:
  name: app-secret
  namespace: crypto-spread-ranking
type: Opaque
data:
  jwt-token: QUJDMTIz  # base64 encoded "ABC123"
stringData:
  redis-password: ""  # Redis password if needed